import Control from '@/common/components/control';

export default class Winner extends Control {

  constructor(parentNode: HTMLElement | null) {
    super(parentNode, 'tbody', '', '');
  }

  renderWinner() {
      
  }
}