const baseUrl = 'https://race-async.herokuapp.com';

const garage = `${baseUrl}/garage`;
const engine = `${baseUrl}/engine`;
const winners = `${baseUrl}/winners`;

export {
  baseUrl,
  garage,
  engine,
  winners,
};
