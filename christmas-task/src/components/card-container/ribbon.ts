import { Control } from '@/common/components/control';

export default class Ribbon extends Control {
  constructor(parentNode: HTMLElement) {
    super(parentNode, 'div', 'ribbon', '');
  }
}
