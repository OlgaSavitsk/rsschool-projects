export interface ISettingsTree {
  sound: boolean,
  snow: boolean,
  tree: string,
  bg: string
}

export const settingaTree = {
  sound: false,
  snow: false,
  tree: '1',
  bg: '1',
};
